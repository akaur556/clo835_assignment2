apiVersion: apps/v1
kind: Deployment
metadata:
  name: webapp
  labels:
    tier: frontend
spec:
  selector:
    matchLabels:
     tier : frontend
  template:
    metadata:
      labels:
        tier: frontend
    spec:
      containers:
      - image: imageURI
        name: webapp
        ports:
        - containerPort: 8080